#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: first integer
 * @b: second integer
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int mul;

	for mul = (a * b)
	{
		return (mul);
	}
	return (0);
}
