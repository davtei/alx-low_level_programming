#include "main.h"

/**
 * mod - function that returns the remainder of a division operation
 * @a: number to be divided
 * @b: divisor
 * Return: remainder
 */
int mod(int a, int b)
{
	return (a % b);
}
